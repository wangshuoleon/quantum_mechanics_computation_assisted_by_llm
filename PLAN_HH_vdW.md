# 两个基态氢原子之间 vdW 力的 Mathematica 8 计算规划

## 0. 任务定位

- 目标：用 Mathematica 8 计算两个基态氢原子（H(1s)–H(1s)）之间的色散相互作用势能 V(R) = −C₆/R⁶，并由此得到 vdW 吸引力 F(R) = −dV/dR。
- 理论框架：二阶定态微扰（London 1930）。用户已熟悉微扰论，下文不再铺垫推导，只列可直接落码的公式与收敛策略。
- 单位：全程原子单位（ℏ = m_e = e = 1, 4πε₀ = 1），最后再换回 SI。

---

## 1. 物理模型

取两原子连线为 z 轴，核间距 R。

未微扰哈密顿量：

```
H0 = h(1) + h(2),   h = -1/2 ∇² - 1/r
基态 |0> = |1s>₁|1s>₂,   E0 = -1/2 - 1/2 = -1  Hartree
```

偶极–偶极相互作用（多极展开领头项，R ≫ a₀）：

```
V = (1/R³) ( x₁ x₂ + y₁ y₂ - 2 z₁ z₂ )
```

一阶修正 ⟨0|V|0⟩ = 0（单原子 1s 偶极矩为零）。

二阶修正：

```
E⁽²⁾(R) = - Σ_{n≠0} |⟨n|V|0⟩|² / (En - E0)
        = - C₆ / R⁶
```

因此 C₆ 是唯一需要算的数。

---

## 2. 角向约化（关键一步，码前必须先做）

V 是两个原子偶极矩的双线性型。从 |1s1s⟩（两个 l=0）出发，偶极算符的选择定则要求**两个原子同时被激发到 p 态**（单原子激发的中间态矩阵元为零，因为 ⟨1s|z|1s⟩=0）。

对所有 m₁, m₂ 求和后，角向因子全部积掉，仅剩径向矩阵元：

```
I_n = ⟨R_{10}| r | R_{n1}⟩        （径向偶极矩阵元，n ≥ 2）

C₆(bound) = (2/3) · Σ_{n₁=2}^{N} Σ_{n₂=2}^{N}
              I_{n₁}² · I_{n₂}² / ( 1 - 1/(2 n₁²) - 1/(2 n₂²) )
```

分母即 E(n₁p)+E(n₂p)−2E(1s) = 1 − 1/(2n₁²) − 1/(2n₂²)。

> 系数 2/3 来自 x₁x₂ + y₁y₂ − 2z₁z₂ 在 m 求和后的各向同性收缩；交叉项（x z 等）因 m 正交消失。

---

## 3. 氢原子径向波函数（原子单位）

```
R_{10}(r) = 2 e^{-r}

R_{n1}(r) = sqrt( (2/n)³ · (n-2)! / (n+1)! )
          · (2r/n) · e^{-r/n} · L_{n-2}^{3}(2r/n)
```

L 是关联拉盖尔多项式 `LaguerreL[k, α, x]`。

---

## 4. 计算步骤（Mathematica 8 落地顺序）

### Step 1 — 符号径向矩阵元 I_n

让 Mathematica 直接积分：

```mathematica
R10[r_] = 2 Exp[-r];
Rn1[n_, r_] = Sqrt[(2/n)^3 (n-2)!/(n+1)!] (2 r/n) Exp[-r/n]
              LaguerreL[n-2, 3, 2 r/n];
I[n_?NumericQ] := NIntegrate[R10[r] r Rn1[n, r] r^2, {r, 0, Infinity},
                            Method -> "ExtrapolatingOsculatory", PrecisionGoal -> 12];
```

- 先用 `Integrate[..., {r,0,∞}]` 试一次符号解；算不动就退回上面的 NIntegrate。
- 先验证 n=2：<1s|r|2p> 的解析值已知，作为 sanity check。

### Step 2 — 束缚态双重求和

```mathematica
denom[n1_, n2_] = 1 - 1/(2 n1^2) - 1/(2 n2^2);
C6bound[Nmax_] := (2/3) Sum[ I[n1]^2 I[n2]^2 / denom[n1, n2],
                             {n1, 2, Nmax}, {n2, 2, Nmax} ];
```

- 取 Nmax = 5, 10, 20, 40 各算一次，画 C₆(Nmax) 收敛曲线。
- 预期：束缚态求和会**系统性偏低**，因为漏掉了两个连续态通道。

### Step 3 — 连续态修正（两条路任选）

**路 A（外推，轻量）**：n 大时 I_n ~ n⁻³ 衰减，分母 ~ 1。用 Nmax→∞ 的尾部渐近行为做 Richardson / 指数外推，把 Step 2 的截断值补到完整值。

**路 B（Dalgarno–Lewis，精确基准，推荐用于交叉验证）**：

解非齐次径向方程（l=1 通道）：

```
[-1/r² d/dr(r² d/dr) + 2/r² - 2/r + 1/2] g(r) = - r · R_{10}(r)
```

这等价于把所有束缚+连续激发态一起求和。g(r) 有解析解（Dalgarno & Lewis 1955），Mathematica 用 `DSolve` 试符号解；失败就 `NDSolve`（在 r ∈ [0, 30] 上，注意原点正则条件 g(0)=0）。

然后通过 g(r) 构造 C₆ 的一维积分表达式（比双重求和快且自动含连续态），作为精确基准。

### Step 4 — 势能与力

```mathematica
V[R_, c6_] = -c6 / R^6;
F[R_, c6_] = -6 c6 / R^7;   (* 负号表示吸引力，沿 -R 方向 *)
```

- 画 V(R)、F(R) 曲线（R ∈ [3, 20] a₀）。
- 标上 C₆ 的收敛值与精确值。

### Step 5 — 单位换算

```
1 a.u. C₆ = 1 Hartree · a₀⁶
         = 11.209 6 × 10⁻⁸⁰ J·m⁶   （换算系数，码里写死并注释来源）
```

F 的 SI 单位：N（牛顿）。

---

## 5. 验证基准

| 量 | 公认值 |
|---|---|
| C₆(H–H) | **6.499 026 8** a.u.（Λ≈6.5 量级；London / 精确量子力学结果） |
| 静态偶极极化率 α(0) | 9/2 = 4.5 a.u.（Dalgarno–Lewis 单独验证） |

- Step 3 路 B 的结果应与 6.4990268 在 10⁻⁴ 量级一致。
- Step 2 的束缚态求和应收敛到该值的 80–90%，缺口即连续态贡献。

---

## 6. Mathematica 8 注意事项

1. 不要用 v9+ 才有的语法（如新版 `Assuming` 选项、`Groupings`、`RandomVariate` 的高级用法）。
2. 符号 `Integrate` 带 `LaguerreL` 时若卡住，立即切 `NIntegrate`，不要让内核挂死。
3. 高精度用 `N[expr, 30]`；`NIntegrate` 显式给 `PrecisionGoal -> 12`。
4. 双重 Sum 在 Nmax=40 时约 1600 项，秒级完成；不要盲拉到 Nmax=200。
5. 连续态外推时，记得 I_n 是**实数正**，避免被相位符号干扰。

---

## 7. 交付物

1. `HH_vdW.nb`（或 `.m` 脚本）：按 Step 1–5 组织，每步带注释与中间结果输出。
2. 两张图：
   - C₆ vs Nmax 收敛曲线；
   - V(R)、F(R) vs R（a₀ 与 SI 双坐标轴）。
3. 一页结果记录：C₆（束缚态 / 含连续态 / 精确基准），以及 1 nm、0.5 nm 处的 vdW 力数值。

---

## 8. 待用户确认的选项

- [ ] 是否需要同时给出 Dalgarno–Lewis 的 g(r) 解析推导（路 B），还是只做路 A 外推 + 用已知值校准？
- [ ] 中间态基组：纯氢原子解析波函数（本规划默认），还是要加数值基组对角化作为第三条独立路径？
- [ ] 是否需要推广到任意 R 的短程排斥项（Born–Oppenheimer 完整势曲线），还是只做长程 vdW 尾部？
